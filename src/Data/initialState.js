const initialState = {
  capacity: 100,
  temperature: 20,
  flow: 10,
  velocity: null,
  pressureDrop: null,
  allowedPressureDrop: 300,
  allowedVelocity: 2,
  pipe: 15,
  delta: 4
}

export default initialState;